kind: Service
apiVersion: v1
metadata:
  name: slide-manager
  labels:
    app: slide-manager
spec:
  selector:
    app: slide-manager
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: slide-manager
  labels:
    app: slide-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slide-manager
  template:
    metadata:
      labels:
        app: slide-manager
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "slide-manager"
        dapr.io/app-port: "50001"
        dapr.io/app-protocol: "grpc"
        dapr.io/log-level: "debug"
    spec:
      containers:
      - name: slide-manager
        image: ghcr.io/hello-slide/slide-manager@sha256:0732841de9bac2e5ac3bbcc2384fd1ae79c8e338d31af96f9be0660a6a8f1374
        env:
        - name: KEY
          valueFrom:
            secretKeyRef:
              name: slide-manager-secret
              key: json
        - name: SLIDE_CONFIG
          value: "slide-config-state"
        - name: TOKEN_MANAGER
          value: "token-manager"
        ports:
        - containerPort: 3000
        imagePullPolicy: Always
